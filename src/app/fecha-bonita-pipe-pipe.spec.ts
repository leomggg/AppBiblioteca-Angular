import { FechaBonitaPipePipe } from './fecha-bonita-pipe-pipe';

describe('FechaBonitaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FechaBonitaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
